# Runtime notebooks

This folder is reserved for public-safe notebooks and notebook runbooks.

Current local artifact prepared in chat: `DOE_MASTER_ML_PIPELINE_EQUATION_FIRST_V4.ipynb`.

V4 is an equation-first approved-runtime notebook. It standardizes the four local workbook inputs, computes physics/equation features before ML, trains hydrate-saturation regression, trains secondary water-saturation regression, and exports a rule-based occurrence screen.

Do not commit approved workbook rows, unrestricted row-level predictions, local configs, or fitted model objects. Those stay in ignored runtime folders.
